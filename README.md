# ナショナルエコノミー メシア - オンライン対戦ゲーム# national-economy-game

WebSocket を使用したリアルタイム対戦ゲーム（2〜4人対応）

## 📋 プロジェクト概要

このプロジェクトは、**DB なし、Vercel 無料枠対応**の前提で、メモリ上でゲーム状態を管理するオンライン対戦ゲームです。

- **技術スタック**: Next.js + TypeScript + WebSocket (ws)
- **サーバーモデル**: 独立した Node.js WebSocket サーバー + Next.js UI
- **デプロイ**: Vercel（無料枠対応、heart-beat で Sleep 対策）
- **ゲーム状態**: メモリ管理（DB なし）

---

## 🚀 環境構築手順

### ステップ 1: 前提条件

- **Node.js**: v18 以上
- **npm**: v9 以上
- **Git**: インストール済み

確認コマンド:

```bash
node --version   # v18.x.x 以上
npm --version    # v9.x.x 以上
```

### ステップ 2: リポジトリクローンと依存パッケージのインストール

```bash
cd /Users/bobsup/Projects/national-economy-game
npm install
```

### ステップ 3: 開発環境の起動

**方式A: Next.js UI のみ起動**（WebSocket サーバーなし）

```bash
npm run dev
```

→ `http://localhost:3000` で UI にアクセス可能

**方式B: WebSocket サーバー + Next.js UI を同時起動（推奨）**

```bash
npm run dev:all
```

→ 別々のターミナルで起動します:

- Next.js: `http://localhost:3000`
- WebSocket サーバー: `ws://localhost:3001`

**方式C: WebSocket サーバーのみ起動**

```bash
npm run dev:ws
```

→ `ws://localhost:3001` でリッスン

---

## 📁 プロジェクト構造

```
national-economy-game/
├── src/
│   ├── app/
│   │   ├── layout.tsx           # ルートレイアウト
│   │   ├── page.tsx             # ホームページ
│   │   └── api/                 # API エンドポイント（将来用）
│   ├── lib/
│   │   ├── game/
│   │   │   ├── GameState.ts     # ゲーム状態管理
│   │   │   └── RoomManager.ts   # ルーム・接続管理
│   │   └── types/
│   │       └── index.ts         # 型定義
│   └── components/              # React コンポーネント（将来用）
├── server.ts                    # WebSocket サーバー
├── package.json
├── tsconfig.json
├── next.config.js
└── README.md
```

---

## 🎮 ゲーム機能（フェーズ別実装計画）

### Phase 1: 基盤整備 ✅ 実装済み

- [x] Next.js プロジェクト初期化
- [x] WebSocket サーバー（`server.ts`）
- [x] ルーム管理（作成・参加・退出）
- [x] Heart-beat（5秒ごとの ping/pong で Idle 防止）
- [x] 入退室のブロードキャスト

### Phase 2: GameState 基礎 ✅ 実装済み

- [x] GameState クラス設計
- [x] プレイヤー管理
- [x] 山札・手札・捨て札管理
- [x] ターン管理
- [x] 初期化処理

### Phase 3: ミニマムゲーム（β版） 🔄 実装中

- [ ] ターン処理の実装
- [ ] 簡易アクション（カード引く、コイン獲得）
- [ ] WebSocket ブロードキャスト
- [ ] シンプルな UI

### Phase 4: 正式ルール実装

- [ ] 全カード効果
- [ ] 建設・支払い・リソース管理
- [ ] ラウンド処理
- [ ] 最終得点計算

### Phase 5: UI/UX 改善

- [ ] 手札 UI
- [ ] ドラッグ操作
- [ ] アニメーション

### Phase 6: 負荷・運用改善

- [ ] キャッシュ戦略
- [ ] データベース導入（将来）

---

## 🔌 WebSocket 接続仕様

### 接続 URL

```
ws://localhost:3001/?roomId=ABC123&playerId=player1&playerName=Alice
```

**パラメータ:**

- `roomId`: 6文字のルーム ID（新規作成時はサーバーが自動生成）
- `playerId`: プレイヤーの一意識別子
- `playerName`: プレイヤー表示名

### メッセージ形式

すべてのメッセージは JSON 形式:

```typescript
{
  type: string; // メッセージタイプ
  payload: object; // ペイロード
  timestamp: number; // UNIXタイムスタンプ
}
```

### メッセージタイプ一覧

#### クライアント送信

**pong** - Heartbeat 応答

```json
{
  "type": "pong",
  "payload": {},
  "timestamp": 1234567890
}
```

**ready** - ゲーム開始準備完了

```json
{
  "type": "ready",
  "payload": {
    "playerId": "player1"
  },
  "timestamp": 1234567890
}
```

**action** - ゲームアクション

```json
{
  "type": "action",
  "payload": {
    "playerId": "player1",
    "actionType": "draw_card",
    "data": {}
  },
  "timestamp": 1234567890
}
```

#### サーバー送信

**ping** - Heartbeat（5秒ごと）

```json
{
  "type": "ping",
  "payload": {},
  "timestamp": 1234567890
}
```

**player_joined** - プレイヤー参加

```json
{
  "type": "player_joined",
  "payload": {
    "playerId": "player1",
    "playerName": "Alice",
    "players": [...]
  },
  "timestamp": 1234567890
}
```

**player_ready** - プレイヤー準備完了

```json
{
  "type": "player_ready",
  "payload": {
    "playerId": "player1",
    "players": [...]
  },
  "timestamp": 1234567890
}
```

**game_started** - ゲーム開始

```json
{
  "type": "game_started",
  "payload": {
    "gameState": {...},
    "currentPlayer": {...}
  },
  "timestamp": 1234567890
}
```

**turn_changed** - ターン変更

```json
{
  "type": "turn_changed",
  "payload": {
    "currentPlayer": {...},
    "round": 1,
    "gameState": {...}
  },
  "timestamp": 1234567890
}
```

---

## 📊 GameState データ構造

```typescript
interface GameState {
  roomId: string;
  players: Map<string, Player>;
  deck: Card[]; // 山札
  discard: Card[]; // 捨て札
  currentPlayerIndex: number; // 現在のプレイヤーインデックス
  round: number; // 現在のラウンド
  phase: "lobby" | "ingame" | "finished";
  maxRounds: number; // 最大ラウンド数
}

interface Player {
  id: string;
  name: string;
  ready: boolean;
  coins: number;
  food: number;
  population: number;
  hand: Card[];
  buildings: Card[];
}

interface Card {
  id: string;
  name: string;
  cost: number;
  effect: string;
}
```

---

## 🔧 Vercel デプロイ対応

### 現在のアーキテクチャの課題

Vercel Functions は WebSocket をネイティブサポートしていないため、本番環境では以下の対応が必要です：

### 推奨デプロイ方式（複数選択肢）

**Option 1: Socket.io + HTTP ポーリング（推奨）**

```bash
npm install socket.io
# server.ts を Socket.io に変更
```

→ Socket.io は Vercel + HTTP ポーリングで動作

**Option 2: Vercel KV + Pub/Sub**

- Vercel KV（Redis）で Pub/Sub を実装
- HTTP API エンドポイントでメッセージ送受信

**Option 3: Railway / Render などでホスト**

- 別サーバーで WebSocket サーバーを稼働
- Vercel UI + 外部 WebSocket サーバー

**推奨: Option 1（Socket.io）**

---

## 🧪 テスト・ローカル開発

### WebSocket クライアントテスト

ローカル開発での テスト例:

```javascript
// ブラウザコンソールで実行
const ws = new WebSocket(
  "ws://localhost:3001?roomId=TEST01&playerId=p1&playerName=Alice"
);

ws.onopen = () => {
  console.log("Connected");
  // ready メッセージ送信
  ws.send(
    JSON.stringify({
      type: "ready",
      payload: { playerId: "p1" },
      timestamp: Date.now(),
    })
  );
};

ws.onmessage = (event) => {
  console.log("Message:", JSON.parse(event.data));
};
```

### ログ確認

WebSocket サーバーのログ:

```bash
npm run dev:ws
# ターミナルに詳細ログが表示される
```

---

## 🛠️ 開発ガイド

### 新しいメッセージタイプを追加する手順

1. **`src/lib/types/index.ts`** に型を追加

```typescript
export interface MyNewMessage extends GameMessage {
  type: "my_new_type";
  payload: {
    /* ... */
  };
}
```

2. **`server.ts`** にハンドラーを追加

```typescript
case 'my_new_type':
  handleMyNewType(ws, message, room, roomManager);
  break;

function handleMyNewType(...) {
  // 実装
}
```

3. **クライアント側** で メッセージ受信・送信を実装

### 新しいアクションを追加する手順

1. **`src/lib/game/GameState.ts`** にメソッドを追加

```typescript
myNewAction(playerId: string): void {
  // ゲーム状態を更新
}
```

2. **`server.ts`** の `handleAction` に case を追加

```typescript
case 'my_new_action':
  room.myNewAction(playerId);
  // ブロードキャスト
  roomManager.broadcastToRoom(room.roomId, {...});
  break;
```

---

## 📈 性能・スケーリング

### 現在の制限

- **最大ルーム数**: 20 程度
- **最大プレイヤー数**: ルームあたり 4 人
- **最大同時接続**: 80 人程度
- **メモリ使用量**: ルームあたり約 1MB

### 最適化戦略

1. **ルーム削除**: 10 分以上プレイヤーなしのルームを自動削除
2. **メッセージ圧縮**: 大きなペイロードは必要に応じて圧縮
3. **DB への移行**: 将来、Redis/Supabase への移行を想定

---

## 🐛 トラブルシューティング

### WebSocket 接続ができない

```bash
# サーバーが起動しているか確認
npm run dev:ws
# ws://localhost:3001 でリッスンしているはず
```

### メッセージが届かない

1. ネットワークタブで WebSocket フレームを確認
2. サーバーログで `[WebSocket]` メッセージを確認
3. メッセージ形式が正しいか確認（JSON、型など）

### ゲーム状態が同期しない

1. `roomManager.broadcastToRoom()` が呼び出されているか確認
2. クライアント側で受信ハンドラーが実装されているか確認

---

## 📝 次のステップ（Phase 3 以降）

1. **ミニマムゲーム実装**
   - 簡易アクション（カード引く、ターン進行など）
   - シンプルなテスト UI

2. **ゲームロジック実装**
   - 全カード効果
   - リソース管理
   - 得点計算

3. **UI/UX 改善**
   - プロフェッショナルな UI デザイン
   - ゲーム盤面表示
   - リアルタイムアニメーション

4. **本番環境対応**
   - Socket.io への切り替え
   - Vercel デプロイ
   - エラーハンドリング強化

---

## 💬 サポート

問題が発生した場合は、README のトラブルシューティングセクションを参照してください。
